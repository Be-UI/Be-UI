import paginationComponents from './src/pagination'
import { withInstall } from "@be-ui/utils"
const BePagenation = withInstall(paginationComponents)
export {
  BePagenation
}
export default BePagenation
