import { BeMessage, resetMessageInstMap } from './src/be-message-service'

export default {
  service: BeMessage,
  reset: resetMessageInstMap,
}
export const service = BeMessage
export const reset = resetMessageInstMap
